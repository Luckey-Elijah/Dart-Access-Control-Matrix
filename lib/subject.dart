import 'object.dart';
import 'roles.dart';

class Subject extends Object {
  // ---- FIELDS ---- //

  /// Identifier of the the [Subject].
  int id;

  /// The [Role] of the the [Subject].
  Role role;
}
